{
    "routes": [
      {
        "method": "GET",
        "path": "/users/me",
        "handler": "User.me",
        "config": {
          "policies": []
        }
      },
      {
        "method": "GET",
        "path": "/users",
        "handler": "User.find",
        "config": {
          "policies": []
        }
      }
    ]
}